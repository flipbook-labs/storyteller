---
bump: patch
category: Dependencies
---

Update the docs site's dependencies to resolve all open Dependabot alerts. Bumps `ws`, `lodash`, `webpack`, `ajv`, and `@babel/runtime`, and pins `serialize-javascript`, `uuid`, and `prismjs` to patched versions via `overrides` (avoiding a Docusaurus downgrade).
